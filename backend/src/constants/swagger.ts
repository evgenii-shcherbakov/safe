export enum MainOperation {
  TEST = 'health check',
}

export enum SecretsOperation {
  GET = 'get all secrets',
  POST = 'create secret',
  PUT = 'update secret',
  DELETE = 'delete secret',
}

export enum CardsOperation {
  GET = 'get all cards',
  POST = 'create card',
  PUT = 'update card',
  DELETE = 'delete card',
}
